function add(a,b) {
    return a+b;
}

function multiply(a,b) {
    return a*b;
}

module.exports = {
    //ha a kulcs és az érték neve megegyezik, akkor nem kell kétszer leírni
    add, // add: add
    multiply, // multiply: multiply
};